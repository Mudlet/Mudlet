# Coordinator's own re-runs of agent findings

## F-B1-1 (ESC as last byte before the 300 ms flush loses the colour code) - CONFIRMED
Run 2026-09-19 ~14:40 UTC on display :79, tree dbbf040c3, script verify-me/b1/esc-last.lua
(the agent's five feedTelnet lines, then a dump of the last lines with getCurrentLine()).
Mirror log excerpt (verify-me/b1/mudlet.log):
    VERIFY[3] text=whole1:RED1:end        <- control, delivered in one read: correct
    VERIFY[4] text=csiA:
    VERIFY[5] text=[1;31mRED1:end         <- ESC was the last byte of the previous read: parameters printed as text
    VERIFY[6] text=csiB:
    VERIFY[7] text=RED1:end               <- ESC first byte of the second read: correct
Screenshot: verify-me/b1/shot.png. Severity Major stands: same visible defect as #10766 in a one-byte window.

## F-E1-4 (`mudlet --version` aborts when there is no display) - CONFIRMED, pre-existing
Run 2026-09-19 on tree 85d814292: `env -u DISPLAY QT_QPA_PLATFORM= ./build-linux-debug-nosan/src/mudlet --version`
exits 134 (SIGABRT) after Qt's "could not connect to display / Available platform plugins are: ..." message
(verify-me/version-nodisplay.txt). With QT_QPA_PLATFORM=offscreen it prints the version and exits 0.
Not a regression: in Mudlet-5.0.1 `src/main.cpp` handles `--version` at the same point (after the
QApplication is constructed; the option is declared at line 394 and read at line 526, versus 385 and 517
on development, an identical structure). Severity: Minor, tag Note/pre-existing unless the tracker has it.

## F-E1-1 / F-E1-2 (keychain test SEGFAULT, HomeUntouchedTest) - CONFIRMED on dbbf040c3, FIXED by 12b373743
After merging origin/development (12b373743, PR #10858) and rebuilding, `ctest -R '^(CredentialManagerKeychainTest|HomeUntouchedTest|WrapLineRewrapTest)$'`
passes 3/3 (rebaseline-ctest.txt). E1's diagnosis (test-harness use-after-free, not the product) matches the PR's own description.

## F-C1-5 (match-all trigger quadratic in line length) - CONFIRMED on development; 5.0.1 comparison pending
Run on tree 85d814292, display :79, script verify-me/c1/scale.lua (C1's trigger, my own loop; 25/50/100 kB
single lines, both an empty-match pattern and a non-empty one):
    pattern (\w*)   25 kB 0.400 s   50 kB 1.505 s   100 kB 5.843 s
    pattern (\w+)   25 kB 0.397 s   50 kB 1.485 s   100 kB 5.783 s
Each doubling costs ~3.8x, so the per-match work is linear in the line length; the empty-match pattern is
not needed to trigger it. Whether 5.0.1 behaves the same is being measured on a worktree build of the tag.

### F-C1-5 continued: 5.0.1 A/B (Debug, linux-debug-nosan preset for both)
5.0.1 worktree at /home/user/worktrees/v501 (tag Mudlet-5.0.1, app target only, built 15:09 UTC).
First run, machine loaded by three agents (verify-me/c1/v501-timings.txt vs dev-timings.txt):
    (\w*)  25/50/100 kB   5.0.1: 0.261 / 0.973 / 3.718 s     dev: 0.400 / 1.505 / 5.843 s
    (\w+)  25/50/100 kB   5.0.1: 0.251 / 0.937 / 3.636 s     dev: 0.397 / 1.485 / 5.783 s
Interleaved dev/5.0.1/dev/5.0.1, load average 0.5 (verify-me/c1/ab-*.txt), pattern (\w+):
    dev   run1  25 kB 0.315 s   50 kB 1.227 s      5.0.1 run1  25 kB 0.110 s   50 kB 0.380 s
    dev   run2  25 kB 0.329 s   50 kB 1.247 s      5.0.1 run2  25 kB 0.103 s   50 kB 0.394 s
Conclusion so far: the quadratic growth is pre-existing (both trees ~4x per doubling), but development is
about 3x slower than 5.0.1 in absolute terms on this path in Debug builds. Release builds of both are being
made to check whether the constant-factor slowdown survives optimisation before it is reported as a regression.

## F-D1-2 (editor autocomplete offers names carrying wiki markup) - CONFIRMED by data, REGRESSION
`grep -c 'edit | edit source' src/lua-function-list.json`: 669 on 85d814292, 0 on Mudlet-5.0.1.
`git log -S'edit | edit source'` names 5f6ac2513 "Infrastructure: Update autocompletion data in Mudlet (#10356)"
as the commit that introduced them: the autogenerated list scraped the wiki's section-edit links into the
function names, e.g. `"addAreaName[edit | edit source]": "areaID = addAreaName(areaName)"`.
Tracker: open #10816 "Script Editor autocomplete not showing function arguments" (labelled regression) is the
same defect seen from the user's side. The generator (the update-autocompletion workflow) needs the fix; the
file itself is not hand-edited (CLAUDE.md).

## F-D1-1 (pasting newline-only text into a trigger pattern field aborts Mudlet) - CONFIRMED, pre-existing (#10337)
Ran D1's repro script unchanged except for the binary path against the 5.0.1 Debug build on display :79
(verify-me/d1/repro-10337-v501.sh, log verify-me/d1/v501-gdb.log): the 5.0.1 client dies the same way -
ASSERT: "!isEmpty()" in file /opt/qt/6.9.0/gcc_64/include/QtCore/qlist.h, line 676
backtrace through SingleLineTextEdit::insertFromMimeData. The line is the same in both trees
(`text.split(QRegularExpression("[\r\n]"), Qt::SkipEmptyParts).first()`, src/SingleLineTextEdit.cpp:68 in 5.0.1,
:69 on development). Not a regression. The abort is Qt's Q_ASSERT in qlist.h, which a Release build compiles
out: there `first()` on an empty list is undefined behaviour rather than a guaranteed abort, so the user-facing
severity on shipped builds is "may crash or paste garbage"; to be checked on the Release binary once built.

## F-E1-3 (connection dialog's first frame describes a profile other than the highlighted one) - CONFIRMED, pre-existing
Copied V2's two-profile HOME (/tmp/tmp.fxprg1Y6Cd, profiles "V2 Creds" and "V2 Offline NoAddress") and opened the
dialog with no arguments on both binaries, screenshot at 12 s with no input (verify-me/e1/v501-first-frame.png,
dev-first-frame.png): identical - "V2 Creds" highlighted in My games, the Connect to pane reading
Profile name "Mudlet self-test", Server address mudlet.org, Port 23, the self-test description, Connect enabled.
5.0.1 does the same, so this is not a regression of a commit in range; closed #10818 is the nearest tracker entry.

### F-D1-1 continued: Release build behaviour
Same repro against the development Release binary (build-linux-release/src/mudlet, linux-release preset,
verify-me/d1/repro-10337-release.sh, log verify-me/d1/release-gdb.log): "Thread 1 received signal SIGSEGV" on the
paste. So without the Debug assertion the client segfaults instead of aborting - a hard crash on shipped-style
builds, not merely a Debug artefact. 5.0.1 Release to be checked once its build finishes (same code line).

### F-C1-5 continued: development Release (linux-release preset), machine loaded by three agents
    (\w*)  25/50/100 kB: 0.120 / 0.447 / 1.827 s      (\w+): 0.117 / 0.466 / 1.831 s   (verify-me/c1/release-dev-timings.txt)
Still ~3.9x per doubling. The 5.0.1 Release build is in progress for the interleaved comparison.

## F-B2-1 (MSDP array of tables / array of arrays never reaches Lua) - CONFIRMED, pre-existing, not on the tracker
Ran B2's work-B2/repro-F-B2-1.lua on 85d814292, display :79 (verify-me/b2/mudlet.log): the error console prints
"could not decode msdp.GROUP ... after array element, I expect ',' or ']'  {"NAME":"Fred","HEALTH":"90"}{"NAME":"Barney",...}"
and `msdp.GROUP` / `msdp.AOA` are nil while the single-element array `ONEG[1].NAME == "Solo"` works.
5.0.1's `msdp2Lua()` (src/TLuaInterpreter.cpp:4039 and :4054 at the tag) appends '{' / '[' with no separator
before a sibling element, exactly as development does. Severity Major (a protocol shape from the MSDP spec's own
GROUP example is silently lost); not a regression.

### F-C1-5 concluded: Release A/B, interleaved, load average ~1.1 (verify-me/c1/rel-*.txt), pattern (\w+)
    dev   Release run1  25 kB 0.053 s  50 kB 0.202 s     5.0.1 Release run1  25 kB 0.066 s  50 kB 0.298 s
    dev   Release run2  25 kB 0.048 s  50 kB 0.191 s     5.0.1 Release run2  25 kB 0.069 s  50 kB 0.302 s
Development's Release build is about 30% faster than 5.0.1's on the same match-all line; both still grow ~4x
per doubling. The 3x slowdown seen in Debug builds does not survive optimisation, so it is not a user-facing
regression. Verdict: the quadratic cost is a pre-existing limitation (severity Major as a hang risk on very
long lines, not a regression, no tracker entry); the Improve commits did make this path faster in Release.

### F-D1-1 concluded: 5.0.1 Release binary
Same repro against /home/user/worktrees/v501/build-linux-release/src/mudlet (verify-me/d1/v501-release-gdb.log):
"Thread 1 received signal SIGSEGV". So shipped-style builds of both 5.0.1 and development crash on a newline-only
paste into a pattern field; pre-existing, Blocker-class, tracked as #10337.

## F-G1-1 (a package that uninstalls itself from its own install-time script segfaults) - CONFIRMED, pre-existing, not tracked
Ran G1's repro on display :79 against 85d814292 (verify-me/g1/repro-gdb.log): the package's script body runs during
import, prints "uninstallPackage returned true", and the importer then dies -
  SIGSEGV in XMLimport::importPackage (src/XMLimport.cpp:196) <- Host::installPackage (src/Host.cpp:2972)
  <- TLuaInterpreter::installPackage <- Host::send
G1's own run of the same package against the 5.0.1 Debug binary (work-G1/repro-gdb-501.log) dies at the same line,
so it predates the range. Closed #9557 covered the event-handler flavour only. Severity Blocker (crash), but it needs
a package that removes itself while being installed, so the exposure is package authors rather than ordinary play.

## F-H1-1 (the Linux .deb ships QTagEdit's development files) - CONFIRMED by inspection
`dpkg-deb -c` on the 25 MB package H1 produced with CI/linux-packages/deb/mkdeb.sh against the Release build
(work-H1/mudlet_5.0.0~git20260919.85d814292-1~noble_amd64.deb) lists, beside /usr/bin/mudlet:
    /usr/include/QTagEdit/qtagedit.hpp
    /usr/lib/cmake/QTagEdit/QTagEditTargets.cmake, QTagEditTargets-release.cmake
    /usr/lib/libQTagEdit.a
They come from the submodule's own install() rules (3rdparty/qt-tags-widget/CMakeLists.txt:40-53), which the
packaging's `cmake --install` picks up. Minor, new, packaging only (f4849ed77 is new in this range).

## F-F1-2 (deleting a user window leaves its scroll box orphaned over the main console) - CONFIRMED
Ran F1's work-F1/t23.lua on 85d814292, display :79 (verify-me/f1/mudlet.log, after-t23.png): deleteMiniConsole("uw3")
returns true, the 220x140 scroll box with its scrollbars is then drawn at the top-left of the main console over the
game text, and deleteScrollBox("sbY") answers "scrollbox name 'sbY' not found". Verifier V3 saw the same on 5.0.1.
